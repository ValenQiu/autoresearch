# M1 Handover Note (2026-04-14)

## 1) 当前状态（一句话）

M1 主流程 `PASSIVE -> BASE_ACTIVE -> TASK_ACTIVE -> BASE_ACTIVE` 已可跑通；当前主要问题从“切换失败”转为“切换后晚发稳定性波动 + 上肢目标配置无差值”。

## 2) 关键事实

- controller switch 现在多数场景成功（日志可见 `Successfully switched controllers`）。
- trace 自测已可自动输出诊断：
  - transition 检查；
  - post-switch `pelvis_z` 窗口最小值；
  - `base_to_task_max_delta`（用于判定上肢插值目标是否有效）。
- 最近一次 trace 结果显示：
  - transition 三段 `OK`
  - `post-switch` 短窗口稳定
  - `base_to_task_max_delta=0.000000`（上肢目标与 standby 基本一致）

## 3) 新增能力（本轮）

- runtime trace 参数：
  - `trace_enable`
  - `trace_output_jsonl`
  - `trace_stride`
  - `trace_joint_names_csv`
- 采集源：
  - 状态机事件与切换结果
  - `/tf`（速度、pelvis_z）
  - `/joint_states`（关节角）
  - blend 命令（alpha + pose）
  - policy 输入输出摘要（step 计数 + 统计）
- 新自测：
  - `scripts/selftest_m1_trace_diag.sh`
  - `scripts/m1_trace_diag_events.json`
  - `SELFTEST_MODE=trace_diag`

## 4) 运行入口（新机器）

```bash
cd ~/colcon_ws
SELFTEST_MODE=trace_diag bash src/motion_tracking_controller/scripts/selftest_m1.sh
```

## 5) 产物位置

- trace: `/tmp/m1_trace_diag.*.jsonl`
- log: `/tmp/m1_trace_diag.*.log`

## 6) 下一 Agent 第一优先级

1. 将 post-switch 诊断窗口从 200 ticks 扩展到 500~800 ticks（覆盖“晚发低高度”）。
2. 提供“可见上肢差值”的 task init pose 自测配置，验证 base->task 插值效果。
3. 对 TASK->BASE 门禁参数做最小收敛：确保运行日志参数与 profile 完全一致（先解“生效配置”问题再调参）。
